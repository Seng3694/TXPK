#pragma once

namespace txpk
{
	/**
	* \brief The size constraint type enum.
	* It is used to define which side should be constrained.
	*/
	enum class SizeContraintType
	{
		None,
		Width,
		Height
	};
}
